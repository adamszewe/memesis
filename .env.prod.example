# Production Environment Configuration
# Copy this file to .env and update with your actual values

# Frontend
WEB_PORT=80

# Backend (internal only, not exposed externally)
APP_PORT=8080

# Database Configuration (internal only, not exposed externally)
DB_HOST=db
DB_PORT=5432
DB_NAME=memesis
DB_USER=memesis_user
DB_PASSWORD=your_secure_password_here
DB_SSLMODE=disable
